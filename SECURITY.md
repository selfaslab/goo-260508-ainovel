# 보안 정책

## API 키

- **저장소에는 OpenAI API 키를 넣지 마세요.** (커밋·PR·이슈·위키 모두 금지)
- 로컬은 **`.env.local`** 에만 본인 키를 두세요. (`.gitignore`에 포함됨)
- 배포는 **호스트(Vercel 등)의 비밀 환경 변수**에만 등록하세요.
- 키가 실수로 올라갔다면 즉시 [OpenAI API keys](https://platform.openai.com/api-keys)에서 해당 키를 **폐기(Revoke)** 하고 새로 발급하세요.

## 알려주기

취약점을 발견했다면 이슈로 비공개 연락을 주시거나, 저장소 관리자에게 안전한 채널로 알려 주세요.
